{
  "mcpServers": {
    "vibegate": {
      "type": "stdio",
      "command": "node",
      "args": ["packages/mcp-server/dist/server.js"],
      "env": { "VIBEGATE_ROOT": "${workspaceFolder}" }
    }
  }
}
